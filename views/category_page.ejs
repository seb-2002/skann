<%- include ('partials/_head.ejs') %>

<div class="category-body">

  <!-- ticker tape -->
  <div class="ticker-wrap">
    <div class="ticker">
      <% for (let i = 0; i < 20; i++) { %>

        <div class="ticker__item"> <%=category.ticker%> </div>

      <% } %>
    </div>
  </div>

  <!-- content box -->
  <div class="content-box">

  <!-- bio page -->
    <% if (category.title === "Bio" && category.text) { %>
      
        <% for (let i=0; i < category.text.length; i++) { %>
          
          <% if (category.text[i].href) { %>

            <div class="p-box p-box__link">
              <a href="<%=category.text[i].href%>">
                <p><span class="no-text-decoration"><%=category.text[i].p%></span>&nbsp&nbsp&nbsp→</p>
              </a>
            </div> 
            
            
            <% } else { %>
              
              <div class="p-box">
                  <p><%=category.text[i].p%></p>
              </div> 
              

          <%  } %>

      
        <% } %>
      
    <% } %>

    <% if (category.title === "Performance") { %>
      
        <% for (let perf in category) { %>
          
          <% if (perf !== "title" && perf !== "ticker" && perf !== "href") { %>

              <div class="show-box">
                 <a href="<%=category.href%><%=perf%>">
                  <p><%=category[perf].title%> (<%=category[perf].year%>)&nbsp&nbsp&nbsp→</p>
                 </a>
                <p class="credits"><%=category[perf].credits%></p>
                <img class="performance-img" src="<%=category[perf].src%>" alt="">
              </div>
            
            
            <% } else { %> 

          <%  } %>

        <% } %>
      
    <% } %>

  <!--end content box  -->
  </div>
<!-- end category-body-->
</div>

<!-- language button  -->
<div id="button-box"class="button-box button-box__cat">
  <div class="flex" >
    <form id="homeButton" method="GET">
      <button class="menu-button menu-button__home menu-button__cat" type="button submit" >
        <p id="">🏠</p>
      </button>
    </form>
      <form id="langForm" method="GET">
        <button class="menu-button menu-button__lang menu-button__cat" type="button submit">
          <p id="langButton"></p>
        </button>
      </form>
</div>


<%- include ('partials/_foot.ejs') %>